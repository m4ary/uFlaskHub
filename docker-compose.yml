version: '3.8'

services:
  uflaskhub:
    image: m4ary/uflask-hub:latest
    ports:
      - "4000:8000" # Expose port 4000
    restart: unless-stopped
    volumes:
      - /your/current/path/uFlaskHub:/usr/src/app

# Uncomment below lines if you need a custom network
# networks:
#   mynetwork:
#     driver: bridge
